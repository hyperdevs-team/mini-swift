# frozen_string_literal: true

default_platform(:ios)

platform :ios do
  desc 'Pass all test for main target'
  lane :pass_tests do
	sh 'xcodebuild -scheme MiniSwift-Package -enableCodeCoverage YES test'
	sh "curl -s https://codecov.io/bash | bash -s -- -F ios -J 'Mini'"
  end
end
